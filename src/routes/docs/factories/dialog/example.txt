<script>
	import { createDialog } from "ghostui/dialog";

    const { dialogWindow, openDialog, closeDialog, isExpanded } = createDialog();
</script>

<button on:click={openDialog}>Toggle</button>
{#if $isExpanded()}
    <div use:dialogWindow>
        <button on:click={closeDialog}>Close</button>
    </div>
{/if}